#!/usr/bin/env python3
##
## PERSONAL PROJECT, 2020
## Fractal Tree
#!

import turtle

def tree(len, mem, t):
    if len > mem/10:
        t.forward(len)
        t.right(25)
        tree(len - 10, mem, t)
        t.left(50)
        tree(len - 10, mem, t)
        t.right(25)
        t.backward(len)

def function(len):
    t = turtle.Turtle()
    screen = turtle.Screen()
    screen.setup(len * 10, len * 6)
    t.speed(1000000)
    t.left(90)
    t.up()
    t.backward(len * 3)
    t.down()
    tree(len, len, t)
    screen.exitonclick()

def val_input():
    len = input("What size for the fractal tree ? ")
    try :
        int(len)
    except ValueError :
        print ("Wrong size input")
        exit(84)
    len = int(len)
    function(len)

val_input()